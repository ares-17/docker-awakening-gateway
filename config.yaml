# Docker Awakening Gateway — Configuration
#
# Mount path: /etc/gateway/config.yaml (set via CONFIG_PATH env var)

gateway:
  port: "8080"
  schedule_timezone: "Europe/Rome"
  log_lines: 30
  # discovery_interval: "15s"  # label polling frequency (default: 15s, or DISCOVERY_INTERVAL env var)

containers:

# Example: container with a dependency (postgres starts before web-app)
#  - name: "web-app"
#    host: "app.localhost"
#    target_port: "3000"
#    depends_on: ["postgres"]
#
#  - name: "postgres"
#    target_port: "5432"        # no host needed — dependency-only

# Example: group with round-robin load balancing
# groups:
#   - name: "api-cluster"
#     host: "api.localhost"
#     strategy: "round-robin"   # default: round-robin
#     containers: ["api-1", "api-2"]
